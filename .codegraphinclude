# === SOURCE CODE (Rust workspace — 3 crates) ===
core/
daemon/
cli/

# === OUTER GLUE ===
AGENTS.md
Cargo.toml

# === EXCLUSIONS (apply within scan roots) ===
# DEFAULT_EXCLUDE_PATTERNS covers: __pycache__, .git, .codegraph, .ix,
# .venv, node_modules, target, .idea, .vscode, etc.
#
# Project-specific exclusions (! = exclude):

# Docs — not source
!docs/

# Annotations — ingested separately, not scanned as source
!.annotations/

# Temporary / sniper backups
!.sniper/
!**/.sniper/

# Maat systemic order audits
!.maat/
!**/.maat/

# Large generated data
!annotate.json
!beauty.json
!Cargo.lock
